

;; (defun helm-save ()
;; 	(interactive)
;; 	(helm :sources (helm-def-source--info-files)
;;         :buffer "*helm Info*"
;;         :preselect (and default
;;                         (concat "\\_<" (regexp-quote default) "\\_>")))))
